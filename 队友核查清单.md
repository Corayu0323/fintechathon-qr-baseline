# 赛题五协作核查清单

这份清单给两位都在用 AI 辅助编程（vibe coding）的队友：一位负责因子，一位负责模型。先看自己负责的部分和双方接口，再做定向修改；不需要从头通读整个仓库。

## 先共同读这几份

| 文件 | 重点核对 |
|---|---|
| `README.md` | 项目如何准备数据、生成因子、运行 D/H/S；目前 fixed 流程已实现，rolling/adaptive 尚未实现。 |
| `specs/研究流程spec草案.md` | 因子可用时点、标签成熟时间、缺失行处理、D/H/S 时间边界、评分口径。它是本轮研究设计的主规格。 |
| `configs/experiment_protocol.json` | 代码实际使用的日期、阶段、候选模型和输入变换配置。规格与配置不一致时先报告差异，不要自行挑一套。 |
| `reports/QR基础设施交接说明.md` | 已完成的 D/H/S 结果、当前冻结的 LightGBM + identity 方案，以及结果解释边界。 |

`specs/archive/` 是历史稿，不是当前实现要求。需要追溯讨论时再看，不要把旧版本参数直接搬回代码。

## 因子负责人

主要核查这些文件：

| 文件 | 要看什么 |
|---|---|
| `specs/factor_set_v0.2.json` | 因子名称、公式、输入字段、窗口是否包含当日、所需连续有效日线数、缺失规则和版本。这里是因子清单。 |
| `scripts/factors/formulas/returns.py` | 收益类公式是否与因子清单逐项一致，窗口有没有错一日。 |
| `scripts/factors/formulas/price.py` | 日内收益、振幅、收盘位置的边界条件及零振幅处理。 |
| `scripts/factors/formulas/activity.py` | 量/额相对前期均值的计算、成交量为零等边界；不要把成交额/成交量混成未核验的价格特征。 |
| `scripts/factors/formulas/common.py` | 公共有效行情条件和时间序列窗口实现。 |
| `scripts/factors/build.py` | 按股票和日期计算、跨年 warm-up、按 `(trade_date, ts_code)` 一对一对齐、保持原始行数、版本目录不覆盖。 |
| `scripts/checks/validate_factors.py` | 公式数值、键完整性、缺失掩码、分批与整批一致性、未来数据扰动检查是否覆盖了实际公式。 |

因子代码的责任边界：公式使用题给量价和标记字段；不读取标签，不在原始因子构建阶段做模型 scaler，也不改模型选择与官方评分。

## 模型负责人（你）

主要核查这些文件：

| 文件 | 要看什么 |
|---|---|
| `configs/model_recipes.json` | Ridge、LightGBM 与透明信号基线的参数；修改配置后是否被 run 记录并可追溯。 |
| `configs/experiment_protocol.json` | D 的候选比较、H 的冻结检验和 S 的提交范围是否符合当前实验设计。 |
| `scripts/modeling/config.py` | 配置读取、字段校验，以及 H/S 冻结模型与变换的检查。 |
| `scripts/modeling/models/lightgbm.py` | 训练/早停/最终拟合的数据边界和随机种子是否按配置执行。 |
| `scripts/modeling/models/ridge.py` | Qlib 适配器只接收接口整理后的特征和标签，预测键是否正确。 |
| `scripts/modeling/models/signals.py` | 透明基线方向和输出分数定义。 |
| `scripts/modeling/preprocessing.py` | `identity`、`train_zscore`、`cs_zscore`、`cs_rank` 的统计范围；训练期状态是否保存并复用于预测期。 |
| `scripts/modeling/qlib_bridge.py` | 因子、标签、辅助标记连接键；拟合样本按标签成熟时点筛选；测试集不加载标签。 |
| `scripts/modeling/run_fixed.py` | D/H/S 阶段编排、模型/变换配对、完整预测键、缺失行情回填、S 不评分测试标签。 |
| `scripts/evaluation/official_score.py` | 本地适配是否忠实复现题给官方评分；不要自行改官方综合分。 |
| `scripts/experiments/tracking.py`、`scripts/reporting/build_dashboard.py` | 参数、模型、预测路径和指标能否按 run 留档并从记录生成看板。 |

模型代码的责任边界：监督学习样本资格、训练/验证切分、模型配方、输入变换、拟合与预测、官方评分及实验记录。首轮只执行 fixed D/H/S；不要把研究流程规格里尚未实现的 rolling/adaptive 当成已经可用。

## 两人一起核查的接口

重点对照 `scripts/factors/build.py` 输出与 `scripts/modeling/qlib_bridge.py` 输入：

- 主键始终是 `(trade_date, ts_code)`，连接后不能增行、丢行或错配股票。
- 因子列名及顺序来自 `specs/factor_set_v0.2.json`；因子集版本要随矩阵一起记录。
- 因子只由时点 (t) 已可见的量价数据生成；标签只在模型训练和评价模块使用。
- 行情完整但标签缺失的行仍可参与历史因子窗口及预测，不能进入监督损失。
- 测试预测保留题目要求的全部 key；不合格行按照已约定的低优先级回填规则产生有限分数，不能静默删除。
- scaler 属于模型输入处理；不能改写原始因子矩阵，也不能在 H/S 用未来日期拟合训练期 scaler。

改动后最少做与责任范围对应的检查：

```bash
.venv/bin/python -m scripts.checks.validate_factors
.venv/bin/python -m scripts.checks.validate_model_config
.venv/bin/python -m scripts.checks.validate_pipeline
```

因子负责人先跑因子验收；模型负责人先跑配置验收；涉及两边接口、训练样本或提交文件时再跑完整流程验收。运行结果、数据、模型权重继续留在 `.gitignore` 管理的目录，不提交到仓库。

## 用 AI 改代码时的协作约定

每次只给 AI 一个明确的小任务，并要求它先指出将改哪些文件、对应哪条规格；不要让它顺手重构对方负责的模块。改完后检查 diff，确认没有碰原始数据、输出目录、密钥或无关文件，再运行上面的定向验收。交接时说明改了什么、运行了什么检查、检查结果、还没确认的假设。

`scripts/modeling/qlib_bridge.py`、`scripts/modeling/preprocessing.py` 和 `scripts/modeling/run_fixed.py` 是接口/编排共享文件。改这些文件前先在群里讲明目的和字段变化，避免两个人同时修改后互相覆盖。
